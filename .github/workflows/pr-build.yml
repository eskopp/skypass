name: PR Build

on:
  pull_request:
    branches:
      - main

jobs:
  pr-build:
    name: Lint HTML
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Run htmlhint
        run: npx --yes htmlhint "**/*.html"
